/* Configuration du tableau de bord — modifiable a la main. */
window.COTE_CONFIG = {
  // URL publique du Worker Cloudflare qui declenche la mise a jour
  // (voir worker/update-cote-worker.js pour le deploiement).
  // Laisser vide => le bouton ouvre simplement la page GitHub Actions.
  updateEndpoint: "",
};
